#define CATCH_CONFIG_MAIN
#include <catch2/catch.hpp>

int add(int a, int b) {
    return a + b;
}

TEST_CASE("Dodawanie dwóch liczb", "[add]") {
    REQUIRE(add(1, 1) == 2);
    REQUIRE(add(2, 3) == 5);
    REQUIRE(add(-1, 1) == 0);
}